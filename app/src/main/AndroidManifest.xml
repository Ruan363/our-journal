<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.razor.ourjournal">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".screens.splash.view.SplashActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screens.timeline.view.TimelineActivity"
            android:label="@string/timeline_title"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".screens.login.view.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme" />
        <activity android:name=".screens.link_partner.view.LinkPartnerActivity" />
        <activity android:name=".screens.add_post.view.AddPostActivity" />
        <activity
            android:name=".screens.BaseDrawerActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".screens.signup.SignUpActivity"
            android:label="@string/sign_up"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>